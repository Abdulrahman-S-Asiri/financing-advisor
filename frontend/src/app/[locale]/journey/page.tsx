import{JourneyWorkspace}from"@/components/journey-workspace";import{isLocale,messages}from"@/lib/i18n";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{persona?:string}>}){const[{locale},{persona}]=await Promise.all([params,searchParams]);if(!isLocale(locale))return null;return <JourneyWorkspace locale={locale} m={messages[locale]} stage="start" initialPersona={persona}/>}
