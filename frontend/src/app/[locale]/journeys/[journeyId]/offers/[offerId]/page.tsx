import{OfferDetail}from"@/components/offer-detail";import{isLocale,messages}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string;journeyId:string;offerId:string}>}){const{locale,journeyId,offerId}=await params;if(!isLocale(locale))return null;const m=messages[locale];return <main className="container page"><OfferDetail locale={locale} m={m} jid={journeyId} oid={offerId}/></main>}
