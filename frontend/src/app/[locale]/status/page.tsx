import{StatusDashboard}from"@/components/status-dashboard";import{isLocale,messages}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))return null;const m=messages[locale];return <main className="container page"><div className="pageHeader"><div className="eyebrow">{m.common.demo}</div><h2>{m.status.title}</h2></div><StatusDashboard locale={locale} m={m}/></main>}
