import Link from "next/link";
import { isLocale, messages } from "@/lib/i18n";
import { Card } from "@/components/ui";

export default async function Landing({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))return null;const m=messages[locale];const prefix=locale==="en"?"/en":"";return <main><section className="hero"><div className="container heroGrid"><div><div className="eyebrow">{m.landing.eyebrow}</div><h1>{m.landing.title}</h1><p className="lead">{m.landing.lead}</p><div className="cluster"><Link className="button buttonGold" href={`${prefix}/journey`}>{m.landing.start}</Link><Link className="button buttonGhost" href={`${prefix}/docs`}>{m.nav.docs}</Link></div></div><div className="heroCard"><div className="metric"><strong>100%</strong><span>{m.landing.engine}</span></div><div className="metric"><strong>0/8</strong><span>{m.common.unverified}</span></div><div className="metric"><strong>3</strong><span>{m.landing.personas}</span></div></div></div></section><section className="section"><div className="container"><div className="eyebrow">{m.landing.honesty}</div><h2>{m.landing.steps}</h2><div className="grid3"><Card><h3>{m.landing.engine}</h3><p>{m.landing.engineText}</p></Card><Card><h3>{m.landing.explain}</h3><p>{m.landing.explainText}</p></Card><Card><h3>{m.landing.truth}</h3><p>{m.landing.truthText}</p></Card></div></div></section></main>}
