import{Card,SimulationBadge}from"@/components/ui";import{isLocale,messages}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))return null;const m=messages[locale];return <main className="container page"><Card><SimulationBadge label={m.common.simulation}/><h2>{m.debt.title}</h2><p>{m.debt.body}</p></Card></main>}
