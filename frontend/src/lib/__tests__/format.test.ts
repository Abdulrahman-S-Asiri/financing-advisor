import{describe,expect,it}from"vitest";import{formatMoney,formatPercent}from"@/lib/format";describe("formatters",()=>{it("formats both locales",()=>{expect(formatMoney(1200,"en")).toContain("1,200");expect(formatMoney(1200,"ar")).toContain("١٬٢٠٠")});it("only formats backend ratios for display",()=>expect(formatPercent(.25,"en")).toContain("25%"))});
