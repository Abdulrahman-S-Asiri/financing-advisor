import{describe,expect,it}from"vitest";import{consumeSseResponse,parseSseEvent}from"@/lib/sse";
describe("SSE",()=>{it("parses named frames",()=>expect(parseSseEvent<{ok:boolean}>("id: 2\nevent: done\ndata: {\"ok\":true}")).toEqual({id:"2",event:"done",retry:undefined,data:{ok:true}}));it("handles partial chunks",async()=>{const encoder=new TextEncoder();const response=new Response(new ReadableStream({start(c){c.enqueue(encoder.encode("event: one\ndata: {\"a\":"));c.enqueue(encoder.encode("1}\n\nevent: two\ndata: {\"b\":2}\n\n"));c.close()}}));const frames:string[]=[];await consumeSseResponse(response,x=>frames.push(x));expect(frames).toHaveLength(2)})});
