import{describe,expect,it}from"vitest";import{offerMatchSchema}from"@/lib/schemas";describe("contracts",()=>{it("requires rate verification visibility",()=>{const parsed=offerMatchSchema.safeParse({offer_id:"x",institution:"Bank",product:"P",structure:"murabaha",status:"eligible",monthly_installment:1,apr_effective:.1,total_amount_payable:2,payment_schedule_months:12,reasons:[],conditions:[],near_miss_suggestions:[]});expect(parsed.success).toBe(false)})});
