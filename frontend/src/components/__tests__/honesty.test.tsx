import{render,screen}from"@testing-library/react";import{describe,expect,it}from"vitest";import{SimulationBadge,UnverifiedBadge}from"@/components/ui";describe("honesty surfaces",()=>{it("labels unverified rates",()=>{render(<UnverifiedBadge label="Unverified rate"/>);expect(screen.getByText(/Unverified/)).toBeVisible()});it("labels simulation",()=>{render(<SimulationBadge label="Simulation only"/>);expect(screen.getByText(/Simulation/)).toBeVisible()})});
