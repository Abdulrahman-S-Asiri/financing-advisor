import Link from "next/link";import type{Locale,Messages}from"@/lib/i18n";
export function StageRail({locale,m,active}:{locale:Locale;m:Messages;active:string}){const p=locale==="en"?"/en":"";const items=[["start",m.journey.start,"/journey"],["analysis",m.journey.analysis,"/journey/analysis"],["offers",m.journey.offers,"/journey/offers"],["decision",m.journey.decision,"/journey/decision"]];return <div className="stageRail"><div className="container stages">{items.map(([id,label,path])=><Link key={id} className={`stage ${id===active?"stageActive":""}`} href={`${p}${path}`}>{label}</Link>)}</div></div>}
