import{beforeEach,describe,expect,it}from"vitest";import{useJourneyStore}from"@/stores/journey";describe("journey persistence",()=>{beforeEach(()=>{sessionStorage.clear();useJourneyStore.getState().resetAll()});it("persists the serializable selection",async()=>{useJourneyStore.getState().selectOffer("offer-1");await new Promise(resolve=>setTimeout(resolve,0));expect(sessionStorage.getItem("athar-journey")).toContain("offer-1")})});
